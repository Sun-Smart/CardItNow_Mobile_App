import 'package:flutter/material.dart';

class CreditCardField extends StatelessWidget {
  CreditCardField({Key? key}) : super(key: key);
  final TextEditingController v1 = TextEditingController();
  final TextEditingController v2 = TextEditingController();
  final TextEditingController v3 = TextEditingController();
  final TextEditingController v4 = TextEditingController();
  final TextEditingController v5 = TextEditingController();
  final TextEditingController v6 = TextEditingController();
  final TextEditingController v7 = TextEditingController();
  final TextEditingController v8 = TextEditingController();
  final TextEditingController v9 = TextEditingController();
  final TextEditingController v10 = TextEditingController();
  final TextEditingController v11 = TextEditingController();
  final TextEditingController v12 = TextEditingController();
  final TextEditingController v13 = TextEditingController();
  final TextEditingController v14 = TextEditingController();
  final TextEditingController v15 = TextEditingController();
  final TextEditingController v16 = TextEditingController();

  @override
  Widget build(BuildContext context) {
    return Container(
        decoration: BoxDecoration(
          border: Border.all(color: Colors.black, width: 1),
        ),
        child: Row(
          children: [
            SizedBox(
              width: 10,
              child: TextField(
                decoration: const InputDecoration(
                  border: InputBorder.none,
                ),
                controller: v1,
                onChanged: (v) {
                  if (v.isEmpty) {
                    FocusScope.of(context).previousFocus();
                  } else {
                    FocusScope.of(context).nextFocus();
                  }
                },
              ),
            ),
            SizedBox(
              width: 10,
              child: TextField(
                obscureText: true,
                decoration: const InputDecoration(
                  border: InputBorder.none,
                ),
                controller: v2,
                onChanged: (v) {
                  if (v.isEmpty) {
                    FocusScope.of(context).previousFocus();
                  } else {
                    FocusScope.of(context).nextFocus();
                  }
                },
              ),
            ),
            SizedBox(
              width: 10,
              child: TextField(
                obscureText: true,
                decoration: const InputDecoration(
                  border: InputBorder.none,
                ),
                controller: v3,
                onChanged: (v) {
                  if (v.isEmpty) {
                    FocusScope.of(context).previousFocus();
                  } else {
                    FocusScope.of(context).nextFocus();
                  }
                },
              ),
            ),
            SizedBox(
              width: 10,
              child: TextField(
                obscureText: true,
                decoration: const InputDecoration(
                  border: InputBorder.none,
                ),
                controller: v4,
                onChanged: (v) {
                  if (v.isEmpty) {
                    FocusScope.of(context).previousFocus();
                  } else {
                    FocusScope.of(context).nextFocus();
                  }
                },
              ),
            ),
            SizedBox(
              width: 10,
              child: TextField(
                decoration: const InputDecoration(
                  border: InputBorder.none,
                ),
                controller: v5,
                onChanged: (v) {
                  if (v.isEmpty) {
                    FocusScope.of(context).previousFocus();
                  } else {
                    FocusScope.of(context).nextFocus();
                  }
                },
              ),
            ),
            SizedBox(
              width: 10,
              child: TextField(
                obscureText: true,
                decoration: const InputDecoration(
                  border: InputBorder.none,
                ),
                controller: v6,
                onChanged: (v) {
                  if (v.isEmpty) {
                    FocusScope.of(context).previousFocus();
                  } else {
                    FocusScope.of(context).nextFocus();
                  }
                },
              ),
            ),
            SizedBox(
              width: 10,
              child: TextField(
                obscureText: true,
                decoration: const InputDecoration(
                  border: InputBorder.none,
                ),
                controller: v7,
                onChanged: (v) {
                  if (v.isEmpty) {
                    FocusScope.of(context).previousFocus();
                  } else {
                    FocusScope.of(context).nextFocus();
                  }
                },
              ),
            ),
            SizedBox(
              width: 10,
              child: TextField(
                obscureText: true,
                decoration: const InputDecoration(
                  border: InputBorder.none,
                ),
                controller: v8,
                onChanged: (v) {
                  if (v.isEmpty) {
                    FocusScope.of(context).previousFocus();
                  } else {
                    FocusScope.of(context).nextFocus();
                  }
                },
              ),
            ),
            SizedBox(
              width: 10,
              child: TextField(
                decoration: const InputDecoration(
                  border: InputBorder.none,
                ),
                controller: v9,
                onChanged: (v) {
                  if (v.isEmpty) {
                    FocusScope.of(context).previousFocus();
                  } else {
                    FocusScope.of(context).nextFocus();
                  }
                },
              ),
            ),
            SizedBox(
              width: 10,
              child: TextField(
                obscureText: true,
                decoration: const InputDecoration(
                  border: InputBorder.none,
                ),
                controller: v10,
                onChanged: (v) {
                  if (v.isEmpty) {
                    FocusScope.of(context).previousFocus();
                  } else {
                    FocusScope.of(context).nextFocus();
                  }
                },
              ),
            ),
            SizedBox(
              width: 10,
              child: TextField(
                obscureText: true,
                decoration: const InputDecoration(
                  border: InputBorder.none,
                ),
                controller: v11,
                onChanged: (v) {
                  if (v.isEmpty) {
                    FocusScope.of(context).previousFocus();
                  } else {
                    FocusScope.of(context).nextFocus();
                  }
                },
              ),
            ),
            SizedBox(
              width: 10,
              child: TextField(
                obscureText: true,
                decoration: const InputDecoration(
                  border: InputBorder.none,
                ),
                controller: v12,
                onChanged: (v) {
                  if (v.isEmpty) {
                    FocusScope.of(context).previousFocus();
                  } else {
                    FocusScope.of(context).nextFocus();
                  }
                },
              ),
            ),
            SizedBox(
              width: 10,
              child: TextField(
                decoration: const InputDecoration(
                  border: InputBorder.none,
                ),
                controller: v13,
                onChanged: (v) {
                  if (v.isEmpty) {
                    FocusScope.of(context).previousFocus();
                  } else {
                    FocusScope.of(context).nextFocus();
                  }
                },
              ),
            ),
            SizedBox(
              width: 10,
              child: TextField(
                obscureText: true,
                decoration: const InputDecoration(
                  border: InputBorder.none,
                ),
                controller: v14,
                onChanged: (v) {
                  if (v.isEmpty) {
                    FocusScope.of(context).previousFocus();
                  } else {
                    FocusScope.of(context).nextFocus();
                  }
                },
              ),
            ),
            SizedBox(
              width: 10,
              child: TextField(
                obscureText: true,
                decoration: const InputDecoration(
                  border: InputBorder.none,
                ),
                controller: v15,
                onChanged: (v) {
                  if (v.isEmpty) {
                    FocusScope.of(context).previousFocus();
                  } else {
                    FocusScope.of(context).nextFocus();
                  }
                },
              ),
            ),
            SizedBox(
              width: 10,
              child: TextField(
                obscureText: true,
                decoration: const InputDecoration(
                  border: InputBorder.none,
                ),
                controller: v16,
                onChanged: (v) {
                  if (v.isEmpty) {
                    FocusScope.of(context).previousFocus();
                  } else {
                    FocusScope.of(context).nextFocus();
                  }
                },
              ),
            ),
          ],
        ));
  }
}
